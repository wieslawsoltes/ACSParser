<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:AcsEditor.Controls">
  <Design.PreviewWith>
    <controls:FramePreview Width="300" Height="250" />
  </Design.PreviewWith>
  
  <Style Selector="controls|FramePreview">
    <Setter Property="Template">
      <ControlTemplate>
        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="4">
          <Grid>
            
            <!-- Frame Display Area -->
            <Panel Background="White">
              <SKBitmapControl Name="PART_BitmapControl"
                               Bitmap="{x:Null}"
                               Stretch="Uniform"
                               StretchDirection="Both"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
              
              <!-- Overlay for no frame message -->
              <TextBlock Name="PART_NoFrameText"
                         Text="No frame data"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         FontStyle="Italic"
                         Opacity="0.6"
                         IsVisible="True"/>
            </Panel>
            
          </Grid>
        </Border>
      </ControlTemplate>
    </Setter>
  </Style>
</Styles>